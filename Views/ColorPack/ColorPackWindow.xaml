<UserControl x:Class="AnikiVisualPackCreator.ColorPackWindow"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource WindowBrush}">

    <UserControl.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="14"/>
        </Style>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="PackListScrollBar" TargetType="{x:Type ScrollBar}">
            <Setter Property="Width" Value="11"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
            <Setter Property="Background" Value="#FF0E141A"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="#FF25343E"
                                BorderThickness="1"
                                CornerRadius="5"
                                SnapsToDevicePixels="True">
                            <Track x:Name="PART_Track"
                                   Margin="2"
                                   IsDirectionReversed="True"
                                   ViewportSize="{TemplateBinding ViewportSize}"
                                   Focusable="False">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}"
                                                  CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                                  Focusable="False"
                                                  IsTabStop="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="{x:Type RepeatButton}">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>

                                <Track.Thumb>
                                    <Thumb MinHeight="44" Background="#FF31505A">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                <Border x:Name="ThumbBorder" Background="{TemplateBinding Background}" CornerRadius="4"/>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="ThumbBorder" Property="Background" Value="#FF3A8592"/>
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter TargetName="ThumbBorder" Property="Background" Value="{StaticResource CyanBrush}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>

                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}"
                                                  CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                                  Focusable="False"
                                                  IsTabStop="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="{x:Type RepeatButton}">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource PackListScrollBar}"/>

        <Style x:Key="ColorSwatchButton" TargetType="Button">
            <Setter Property="Width" Value="42"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FF60717D"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Swatch"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <Grid>
                                <Border Margin="3" BorderBrush="#33FFFFFF" BorderThickness="1" CornerRadius="3"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Swatch" Property="BorderBrush" Value="{StaticResource CyanBrush}"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PreviewPlayButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="13,7"/>
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="Cursor" Value="Arrow"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="PreviewPlayBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="PreviewPlayBorder" Property="Opacity" Value="0.94"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="PreviewPlayBorder" Property="Opacity" Value="0.82"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="PreviewPlayBorder" Property="Opacity" Value="0.42"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>

        <Style x:Key="PreviewSceneTab" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="TabBorder"
                                Background="#FF111B22"
                                BorderBrush="#FF2E3B45"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="{StaticResource CyanBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#FF18313A"/>
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="{StaticResource CyanBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PaletteHexBox" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="Width" Value="100"/>
            <Setter Property="MaxLength" Value="7"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Style="{StaticResource CardStyle}"
                Padding="18,14"
                Margin="0,0,0,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="240"/>
                    <ColumnDefinition Width="14"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="14"/>
                    <ColumnDefinition Width="110"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.RowSpan="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Image Width="58"
                               Height="58"
                               Margin="0,0,14,0"
                               Source="/AnikiPackCreator;component/Assets/AppIcon.png"
                               Stretch="Uniform"
                               RenderOptions.BitmapScalingMode="HighQuality"/>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{DynamicResource ColorHeaderTitle}"
                                       Foreground="{StaticResource AccentBrush}"
                                       FontWeight="Bold"
                                       FontSize="21"/>
                            <TextBlock Text="{DynamicResource ColorHeaderSubtitle}"
                                       Foreground="{StaticResource MutedTextBrush}"
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,0">
                        <Button Content="{DynamicResource NewProject}"
                                Click="NewProjectClick"
                                Style="{StaticResource SecondaryButton}"
                                MinWidth="104"/>
                        <Button Content="{DynamicResource OpenProject}"
                                Click="OpenProjectClick"
                                Style="{StaticResource SecondaryButton}"
                                MinWidth="104"
                                Margin="8,0,0,0"/>
                        <Button Content="{DynamicResource SaveProject}"
                                Click="SaveProjectClick"
                                Style="{StaticResource SecondaryButton}"
                                MinWidth="104"
                                Margin="8,0,0,0"/>
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Row="0" Grid.Column="2" VerticalAlignment="Center">
                    <Label Content="{DynamicResource PackName}"/>
                    <TextBox x:Name="PackNameTextBox"
                             Text="{DynamicResource DefaultColorPackName}"
                             TextChanged="MetadataTextChanged"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="4" VerticalAlignment="Center">
                    <Label Content="{DynamicResource Author}"/>
                    <TextBox x:Name="AuthorTextBox"
                             TextChanged="MetadataTextChanged"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="6" VerticalAlignment="Center">
                    <Label Content="{DynamicResource Version}"/>
                    <TextBox x:Name="VersionTextBox"
                             Text="{DynamicResource DefaultVersion}"
                             MaxLength="32"
                             TextChanged="MetadataTextChanged"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="5" Margin="0,8,0,0">
                    <Label Content="{DynamicResource Description}"/>
                    <TextBox x:Name="DescriptionTextBox"
                             Height="48"
                             MaxLength="300"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             TextChanged="MetadataTextChanged"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="370"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="280"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <ScrollViewer.Resources>
                        <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource PackListScrollBar}"/>
                    </ScrollViewer.Resources>
                    <StackPanel>
                        <TextBlock Text="{DynamicResource ColorPaletteTitle}" Style="{StaticResource SectionTitle}"/>
                        <TextBlock Text="{DynamicResource ColorPaletteHint}"
                                   Foreground="{StaticResource MutedTextBrush}"
                                   FontSize="11"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,14"/>

                        <TextBlock Text="{DynamicResource ColorGroupAccents}" FontWeight="SemiBold" Foreground="{StaticResource CyanBrush}" Margin="0,4,0,8"/>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="8"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource ColorPrimaryAccent}" VerticalAlignment="Center" ToolTip="{DynamicResource ColorPrimaryAccentHint}"/>
                            <Button Grid.Column="1" Tag="PrimaryAccent" Style="{StaticResource ColorSwatchButton}" Background="{Binding PrimaryAccentBrush}" Click="ChooseColorClick"/>
                            <TextBox Grid.Column="3" Tag="PrimaryAccent" GotKeyboardFocus="PaletteFieldGotKeyboardFocus" Style="{StaticResource PaletteHexBox}" Text="{Binding PrimaryAccent, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="8"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource ColorSecondaryAccent}" VerticalAlignment="Center" ToolTip="{DynamicResource ColorSecondaryAccentHint}"/>
                            <Button Grid.Column="1" Tag="SecondaryAccent" Style="{StaticResource ColorSwatchButton}" Background="{Binding SecondaryAccentBrush}" Click="ChooseColorClick"/>
                            <TextBox Grid.Column="3" Tag="SecondaryAccent" GotKeyboardFocus="PaletteFieldGotKeyboardFocus" Style="{StaticResource PaletteHexBox}" Text="{Binding SecondaryAccent, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="8"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource ColorFocus}" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Tag="Focus" Style="{StaticResource ColorSwatchButton}" Background="{Binding FocusColorBrush}" Click="ChooseColorClick"/>
                            <TextBox Grid.Column="3" Tag="Focus" GotKeyboardFocus="PaletteFieldGotKeyboardFocus" Style="{StaticResource PaletteHexBox}" Text="{Binding FocusColor, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="8"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource ColorActionButtons}" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Tag="ActionButtons" Style="{StaticResource ColorSwatchButton}" Background="{Binding ActionButtonsColorBrush}" Click="ChooseColorClick"/>
                            <TextBox Grid.Column="3" Tag="ActionButtons" GotKeyboardFocus="PaletteFieldGotKeyboardFocus" Style="{StaticResource PaletteHexBox}" Text="{Binding ActionButtonsColor, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <Grid Margin="0,0,0,14">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="8"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource ColorProgress}" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Tag="Progress" Style="{StaticResource ColorSwatchButton}" Background="{Binding ProgressColorBrush}" Click="ChooseColorClick"/>
                            <TextBox Grid.Column="3" Tag="Progress" GotKeyboardFocus="PaletteFieldGotKeyboardFocus" Style="{StaticResource PaletteHexBox}" Text="{Binding ProgressColor, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <Border BorderBrush="#FF2A3943" BorderThickness="0,1,0,0" Margin="0,0,0,14"/>
                        <TextBlock Text="{DynamicResource ColorGroupInterface}" FontWeight="SemiBold" Foreground="{StaticResource CyanBrush}" Margin="0,0,0,8"/>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="8"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource ColorBackground}" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Tag="Background" Style="{StaticResource ColorSwatchButton}" Background="{Binding BackgroundBrush}" Click="ChooseColorClick"/>
                            <TextBox Grid.Column="3" Tag="Background" GotKeyboardFocus="PaletteFieldGotKeyboardFocus" Style="{StaticResource PaletteHexBox}" Text="{Binding BackgroundColor, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="8"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource ColorBars}" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Tag="Bars" Style="{StaticResource ColorSwatchButton}" Background="{Binding BarsColorBrush}" Click="ChooseColorClick"/>
                            <TextBox Grid.Column="3" Tag="Bars" GotKeyboardFocus="PaletteFieldGotKeyboardFocus" Style="{StaticResource PaletteHexBox}" Text="{Binding BarsColor, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="8"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource ColorMenus}" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Tag="Menus" Style="{StaticResource ColorSwatchButton}" Background="{Binding MenusColorBrush}" Click="ChooseColorClick"/>
                            <TextBox Grid.Column="3" Tag="Menus" GotKeyboardFocus="PaletteFieldGotKeyboardFocus" Style="{StaticResource PaletteHexBox}" Text="{Binding MenusColor, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="8"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource ColorMenuHeader}" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Tag="MenuHeader" Style="{StaticResource ColorSwatchButton}" Background="{Binding MenuHeaderColorBrush}" Click="ChooseColorClick"/>
                            <TextBox Grid.Column="3" Tag="MenuHeader" GotKeyboardFocus="PaletteFieldGotKeyboardFocus" Style="{StaticResource PaletteHexBox}" Text="{Binding MenuHeaderColor, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="8"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource ColorCards}" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Tag="Cards" Style="{StaticResource ColorSwatchButton}" Background="{Binding CardsColorBrush}" Click="ChooseColorClick"/>
                            <TextBox Grid.Column="3" Tag="Cards" GotKeyboardFocus="PaletteFieldGotKeyboardFocus" Style="{StaticResource PaletteHexBox}" Text="{Binding CardsColor, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="8"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource ColorBorder}" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Tag="Border" Style="{StaticResource ColorSwatchButton}" Background="{Binding BorderColorBrush}" Click="ChooseColorClick"/>
                            <TextBox Grid.Column="3" Tag="Border" GotKeyboardFocus="PaletteFieldGotKeyboardFocus" Style="{StaticResource PaletteHexBox}" Text="{Binding BorderColor, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <Grid Margin="0,0,0,14">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="8"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource ColorNotifications}" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Tag="Notifications" Style="{StaticResource ColorSwatchButton}" Background="{Binding NotificationsColorBrush}" Click="ChooseColorClick"/>
                            <TextBox Grid.Column="3" Tag="Notifications" GotKeyboardFocus="PaletteFieldGotKeyboardFocus" Style="{StaticResource PaletteHexBox}" Text="{Binding NotificationsColor, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <Border BorderBrush="#FF2A3943" BorderThickness="0,1,0,0" Margin="0,0,0,14"/>
                        <TextBlock Text="{DynamicResource ColorGroupText}" FontWeight="SemiBold" Foreground="{StaticResource CyanBrush}" Margin="0,0,0,8"/>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="8"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource ColorPrimaryText}" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Tag="PrimaryText" Style="{StaticResource ColorSwatchButton}" Background="{Binding PrimaryTextBrush}" Click="ChooseColorClick"/>
                            <TextBox Grid.Column="3" Tag="PrimaryText" GotKeyboardFocus="PaletteFieldGotKeyboardFocus" Style="{StaticResource PaletteHexBox}" Text="{Binding PrimaryText, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="8"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource ColorSecondaryText}" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Tag="SecondaryText" Style="{StaticResource ColorSwatchButton}" Background="{Binding SecondaryTextBrush}" Click="ChooseColorClick"/>
                            <TextBox Grid.Column="3" Tag="SecondaryText" GotKeyboardFocus="PaletteFieldGotKeyboardFocus" Style="{StaticResource PaletteHexBox}" Text="{Binding SecondaryText, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="8"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource ColorHighlightText}" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Tag="HighlightText" Style="{StaticResource ColorSwatchButton}" Background="{Binding HighlightTextBrush}" Click="ChooseColorClick"/>
                            <TextBox Grid.Column="3" Tag="HighlightText" GotKeyboardFocus="PaletteFieldGotKeyboardFocus" Style="{StaticResource PaletteHexBox}" Text="{Binding HighlightText, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <Button Content="{DynamicResource ColorResetPalette}"
                                Click="ResetPaletteClick"
                                Style="{StaticResource SecondaryButton}"
                                Margin="0,8,0,0"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel>
                        <TextBlock Text="{DynamicResource ColorPreviewTitle}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                        <TextBlock Text="{DynamicResource ColorPreviewHint}" Foreground="{StaticResource MutedTextBrush}" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,0">
                        <RadioButton x:Name="PreviewMainTab"
                                     GroupName="ColorPreviewScene"
                                     IsChecked="True"
                                     Content="{DynamicResource ColorPreviewTabMain}"
                                     Style="{StaticResource PreviewSceneTab}"/>
                        <RadioButton x:Name="PreviewHubTab"
                                     GroupName="ColorPreviewScene"
                                     Content="{DynamicResource ColorPreviewTabHub}"
                                     Style="{StaticResource PreviewSceneTab}"/>
                        <RadioButton x:Name="PreviewMenusTab"
                                     GroupName="ColorPreviewScene"
                                     Content="{DynamicResource ColorPreviewTabMenus}"
                                     Style="{StaticResource PreviewSceneTab}"/>
                        <RadioButton x:Name="PreviewDetailsTab"
                                     GroupName="ColorPreviewScene"
                                     Content="{Binding DetailsPreviewTabText}"
                                     Style="{StaticResource PreviewSceneTab}"/>
                    </StackPanel>

                    <Border Grid.Row="2"
                            Background="#FF0E171D"
                            BorderBrush="#FF2D414B"
                            BorderThickness="1"
                            CornerRadius="7"
                            Padding="11,9"
                            Margin="0,10,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="30" Height="30" Background="{Binding PreviewSelectionBrush}" BorderBrush="#66FFFFFF" BorderThickness="1" CornerRadius="6" Margin="0,0,10,0"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding PreviewSelectionTitle}" Foreground="{StaticResource TextBrush}" FontSize="12" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding PreviewSelectionDescription}" Foreground="{StaticResource MutedTextBrush}" FontSize="10" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Grid>
                    </Border>
<Border x:Name="PreviewResourceHost"
                            Grid.Row="3"
                            Background="#FF090E13"
                            BorderBrush="#FF31404B"
                            BorderThickness="1"
                            CornerRadius="10"
                            Margin="0,12,0,14"
                            ClipToBounds="True">
                        <Viewbox Stretch="Uniform">
                            <Grid Width="960" Height="540" Background="#FF080C10">
<Grid Visibility="{Binding IsChecked, ElementName=PreviewMainTab, Converter={StaticResource BoolToVisibility}}" ClipToBounds="True">
                                    <Image Source="/AnikiPackCreator;component/Assets/ColorPackPreviewBackground.jpeg" Stretch="UniformToFill"/>
                                    <Border Background="{DynamicResource ShadeBackground}"/>
                                    <Border Background="{DynamicResource ShadeLeft}"/>
                                    <Border Height="228" VerticalAlignment="Bottom" Background="{DynamicResource ShadeBottom}"/>

                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="58"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="44"/>
                                        </Grid.RowDefinitions>

                                        <Grid Grid.Row="0">
                                            <Border Background="{DynamicResource TopBar}"/>
                                            <Border Background="{DynamicResource GlassMainBrush}" Opacity="0.76"/>
                                            <Border Height="1.5" VerticalAlignment="Bottom" Background="{DynamicResource SeparatorTopBarBrush}"/>
                                            <Grid Margin="18,0">
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Ellipse Width="34" Height="34" Fill="{DynamicResource BackgroundItem}" Stroke="{DynamicResource FocusGameBorderBrush}" StrokeThickness="1.4"/>
                                                    <StackPanel Margin="9,0,0,0" VerticalAlignment="Center">
                                                        <TextBlock Text="Player" Foreground="{DynamicResource TextBrush}" FontSize="14" FontWeight="Bold"/>
                                                        <TextBlock Text="● Online" Foreground="#FF42D27A" FontSize="8"/>
                                                    </StackPanel>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                    <TextBlock Text="Hub" Foreground="{DynamicResource TextBrush}" FontWeight="SemiBold" Margin="14,0"/>
                                                    <TextBlock Text="Horizontal" Foreground="{DynamicResource TextBrush}" FontWeight="SemiBold" Margin="14,0"/>
                                                    <TextBlock Text="Filter" Foreground="{DynamicResource TextBrush}" FontWeight="SemiBold" Margin="14,0"/>
                                                    <TextBlock Text="Search" Foreground="{DynamicResource TextBrush}" FontWeight="SemiBold" Margin="14,0"/>
                                                    <TextBlock Text="Tools" Foreground="{DynamicResource TextBrush}" FontWeight="SemiBold" Margin="14,0"/>
                                                    <TextBlock Text="Extensions" Foreground="{DynamicResource TextBrush}" FontWeight="SemiBold" Margin="14,0"/>
                                                </StackPanel>
                                            </Grid>
                                        </Grid>

                                        <Grid Grid.Row="1">
                                            <StackPanel Margin="70,96,0,0" VerticalAlignment="Top" HorizontalAlignment="Left">
                                                <TextBlock Text="Yakuza 0" Foreground="{DynamicResource TextBrush}" FontSize="54" FontWeight="Bold"/>
                                                <Border Background="{DynamicResource TextHighlightBrush}" CornerRadius="4" Padding="12,4" HorizontalAlignment="Left" Margin="0,8,0,0">
                                                    <TextBlock Text="DIRECTOR'S CUT" Foreground="{DynamicResource TextColorDarkBrush}" FontSize="14" FontWeight="Bold"/>
                                                </Border>
                                                <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                                                    <Border Background="{DynamicResource ButtonPlayColor}" BorderBrush="{DynamicResource FocusGameBorderBrush}" BorderThickness="1.2" CornerRadius="8" Padding="18,7" Margin="0,0,9,0">
                                                        <TextBlock Text="PLAY" Foreground="{DynamicResource TextBrush}" FontWeight="Bold"/>
                                                    </Border>
                                                    <Border Background="{DynamicResource ButtonDetailsColor}" BorderBrush="{DynamicResource MenuBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="18,7">
                                                        <TextBlock Text="DETAILS" Foreground="{DynamicResource TextBrush}" FontWeight="Bold"/>
                                                    </Border>
                                                </StackPanel>
                                                <Grid Width="250" Height="9" HorizontalAlignment="Left" Margin="0,12,0,0">
                                                    <Border Background="{DynamicResource ProgressBackgroundBrush}" CornerRadius="5"/>
                                                    <Border Width="152" HorizontalAlignment="Left" Background="{DynamicResource ProgressBrush}" CornerRadius="5"/>
                                                </Grid>
                                            </StackPanel>

                                            <StackPanel Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,28,20">
                                                <Border Background="{Binding SecondaryAccentBrush}" BorderBrush="{DynamicResource FocusGameBorderBrush}" BorderThickness="1.2" CornerRadius="12" Padding="12,7" Margin="0,0,0,10">
                                                    <TextBlock Text="♡ Favorite" Foreground="{DynamicResource TextColorDarkBrush}" FontWeight="SemiBold"/>
                                                </Border>
                                                <Border Background="{DynamicResource ButtonBackgroundNoFocus}" BorderBrush="{DynamicResource MenuBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="12,7">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="★" Foreground="{Binding PrimaryAccentBrush}" FontWeight="Bold" Margin="0,0,5,0"/>
                                                        <TextBlock Text="1 / 55" Foreground="{DynamicResource TextBrush}" FontWeight="SemiBold"/>
                                                    </StackPanel>
                                                </Border>
                                            </StackPanel>
<Border Width="285" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,18,20,0"
                                                    Background="{DynamicResource NotificationBackgroundBrush}"
                                                    BorderBrush="{DynamicResource NotificationBorder}"
                                                    BorderThickness="1.1" CornerRadius="7" Padding="9,7">
                                                <Grid>
                                                    <Grid.ColumnDefinitions><ColumnDefinition Width="28"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                                    <Border Width="22" Height="22" Background="{DynamicResource BadgeTrophéeBrush}" CornerRadius="4" VerticalAlignment="Center">
                                                        <TextBlock Text="!" Foreground="{Binding PrimaryAccentBrush}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <StackPanel Grid.Column="1" Margin="8,0,0,0" VerticalAlignment="Center">
                                                        <TextBlock Text="{DynamicResource ColorPreviewNotificationTitle}" Foreground="{DynamicResource TextBrush}" FontWeight="SemiBold" FontSize="10"/>
                                                        <TextBlock Text="{DynamicResource ColorPreviewNotificationText}" Foreground="{DynamicResource TextAltBrush}" FontSize="8"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>

                                            <Grid Height="126" VerticalAlignment="Bottom">
                                                <Border Background="{DynamicResource GameListFrameBackgroundBrush}"/>
                                                <Border Background="{DynamicResource GlassWindowBrush}" Opacity="0.68"/>
                                                <Border Height="1.5" VerticalAlignment="Top" Background="{DynamicResource SeparatorListGameBrush}"/>
                                                <Border Margin="1" BorderBrush="{DynamicResource GlassInnerLineBrush}" BorderThickness="0,1,0,0"/>
<StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="12,8,12,8">
                                                    <Border Width="88" Height="88" CornerRadius="7" Background="#FF343A40" Margin="0,0,10,0" Opacity="0.62"/>
                                                    <Border Width="92" Height="92" CornerRadius="7" Background="#FF454C53" Margin="0,0,10,0">
                                                        <Border Width="28" Height="6" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="8" Background="{Binding SecondaryAccentBrush}" CornerRadius="3"/>
                                                    </Border>
                                                    <Border Width="104" Height="104" CornerRadius="9" Background="#FF2D3339" Margin="0,0,10,0" BorderBrush="{DynamicResource FocusGameBorderBrush}" BorderThickness="3">
                                                        <Grid>
                                                            <Border Margin="5" BorderBrush="{Binding PrimaryAccentBrush}" BorderThickness="1" CornerRadius="5" Opacity="0.72"/>
                                                            <Border Height="5" Margin="9,0,9,8" VerticalAlignment="Bottom" Background="{Binding PrimaryAccentBrush}" CornerRadius="2.5"/>
                                                        </Grid>
                                                    </Border>
                                                    <Border Width="92" Height="92" CornerRadius="7" Background="#FF3E454C" Margin="0,0,10,0"/>
                                                    <Border Width="92" Height="92" CornerRadius="7" Background="#FF292F35" Margin="0,0,10,0"/>
                                                    <Border Width="92" Height="92" CornerRadius="7" Background="#FF4A5056" Margin="0,0,10,0"/>
                                                    <Border Width="92" Height="92" CornerRadius="7" Background="#FF333940">
                                                        <Border Height="8" VerticalAlignment="Top" Background="{Binding SecondaryAccentBrush}" Opacity="0.85"/>
                                                    </Border>
                                                </StackPanel>
                                            </Grid>
                                        </Grid>

                                        <Grid Grid.Row="2">
                                            <Border Background="{DynamicResource BottomBar}"/>
                                            <Border Background="{DynamicResource GlassMainBrush}" Opacity="0.74"/>
                                            <Border Height="1.5" VerticalAlignment="Top" Background="{DynamicResource SeparatorBottomBarBrush}"/>
                                        </Grid>
                                    </Grid>
                                </Grid>
<Grid Visibility="{Binding IsChecked, ElementName=PreviewHubTab, Converter={StaticResource BoolToVisibility}}" ClipToBounds="True">
                                    <Image Source="/AnikiPackCreator;component/Assets/ColorPackPreviewBackground.jpeg" Stretch="UniformToFill" Opacity="0.50"/>
                                    <Border Background="{DynamicResource SecondaryViewBackground}" Opacity="0.91"/>

                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="58"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="44"/>
                                        </Grid.RowDefinitions>

                                        <Grid Grid.Row="0">
                                            <Border Background="{DynamicResource TopBar}"/>
                                            <Border Background="{DynamicResource GlassMainBrush}" Opacity="0.70"/>
                                            <Border Height="1.5" VerticalAlignment="Bottom" Background="{DynamicResource SeparatorTopBarBrush}"/>
                                            <Grid Margin="18,0">
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Ellipse Width="34" Height="34" Fill="{DynamicResource BackgroundItem}" Stroke="{DynamicResource MenuBorderBrush}" StrokeThickness="1.2"/>
                                                    <StackPanel Margin="9,0,0,0" VerticalAlignment="Center">
                                                        <TextBlock Text="Player" Foreground="{DynamicResource TextBrush}" FontSize="14" FontWeight="Bold"/>
                                                        <TextBlock Text="RPG fan" Foreground="{DynamicResource TextAltBrush}" FontSize="8"/>
                                                    </StackPanel>
                                                </StackPanel>
                                                <TextBlock Text="Library      Captures      Media      Store" Foreground="{DynamicResource TextBrush}" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Grid>

                                        <Grid Grid.Row="1" Margin="78,30,78,24">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="166"/>
                                                <RowDefinition Height="28"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>

                                            <Border Width="705" HorizontalAlignment="Center"
                                                    Background="{DynamicResource HubBannerBackgroundBrush}"
                                                    BorderBrush="{DynamicResource HubBannerBorderBrush}"
                                                    BorderThickness="2"
                                                    CornerRadius="13"
                                                    Padding="18">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="1.55*"/>
                                                        <ColumnDefinition Width="18"/>
                                                        <ColumnDefinition Width="1.25*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <Border Width="70" Height="70"
                                                                Background="{DynamicResource HubAchievementsIconBackgroundBrush}"
                                                                BorderBrush="{DynamicResource HubAchievementsIconBorderBrush}"
                                                                BorderThickness="1.5"
                                                                CornerRadius="16">
                                                            <TextBlock Text="★" Foreground="{Binding PrimaryAccentBrush}" FontSize="38" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <StackPanel Margin="16,0,0,0" VerticalAlignment="Center">
                                                            <TextBlock Text="Player" Foreground="{DynamicResource TextHubBrush}" FontSize="24" FontWeight="Bold"/>
                                                            <TextBlock Text="RPG fan" Foreground="{DynamicResource TextHubNameAchievementBrush}" FontSize="15" FontWeight="SemiBold" Margin="0,2,0,0"/>
                                                            <TextBlock Text="Level : 102" Foreground="{DynamicResource TextHubGameAchievementBrush}" FontSize="11" Margin="0,7,0,0"/>
                                                            <TextBlock Text="Total play time : 463d 4h" Foreground="{DynamicResource TextHubDateAchievementBrush}" FontSize="10" Margin="0,3,0,0"/>
                                                        </StackPanel>
                                                    </StackPanel>

                                                    <UniformGrid Grid.Column="2" Rows="2" Columns="3">
                                                        <Border Background="{DynamicResource HubAchievementsBackgroundBrush}" BorderBrush="{DynamicResource HubAchievementsBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="7" Margin="4"><StackPanel><TextBlock Text="GAMES" Foreground="{DynamicResource TextHubGameAchievementBrush}" FontSize="7"/><TextBlock Text="492" Foreground="{DynamicResource TextHubPercentAchievementBrush}" FontSize="18" FontWeight="Bold"/></StackPanel></Border>
                                                        <Border Background="{DynamicResource HubAchievementsBackgroundBrush}" BorderBrush="{DynamicResource HubAchievementsBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="7" Margin="4"><StackPanel><TextBlock Text="INSTALLED" Foreground="{DynamicResource TextHubGameAchievementBrush}" FontSize="7"/><TextBlock Text="28" Foreground="{DynamicResource TextHubPercentAchievementBrush}" FontSize="18" FontWeight="Bold"/></StackPanel></Border>
                                                        <Border Background="{DynamicResource HubAchievementsBackgroundBrush}" BorderBrush="{DynamicResource HubAchievementsBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="7" Margin="4"><StackPanel><TextBlock Text="FAVORITE" Foreground="{DynamicResource TextHubGameAchievementBrush}" FontSize="7"/><TextBlock Text="23" Foreground="{DynamicResource TextHubPercentAchievementBrush}" FontSize="18" FontWeight="Bold"/></StackPanel></Border>
                                                        <Border Background="{DynamicResource HubAchievementsBackgroundBrush}" BorderBrush="{DynamicResource HubAchievementsBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="7" Margin="4"><StackPanel><TextBlock Text="MONTH" Foreground="{DynamicResource TextHubGameAchievementBrush}" FontSize="7"/><TextBlock Text="0m" Foreground="{DynamicResource TextHubPercentAchievementBrush}" FontSize="18" FontWeight="Bold"/></StackPanel></Border>
                                                        <Border Background="{DynamicResource HubAchievementsBackgroundBrush}" BorderBrush="{DynamicResource HubAchievementsBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="7" Margin="4"><StackPanel><TextBlock Text="PLAYED" Foreground="{DynamicResource TextHubGameAchievementBrush}" FontSize="7"/><TextBlock Text="0" Foreground="{DynamicResource TextHubPercentAchievementBrush}" FontSize="18" FontWeight="Bold"/></StackPanel></Border>
                                                        <Border Background="{DynamicResource HubAchievementsBackgroundBrush}" BorderBrush="{DynamicResource HubAchievementsBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="7" Margin="4"><StackPanel><TextBlock Text="COMPLETE" Foreground="{DynamicResource TextHubGameAchievementBrush}" FontSize="7"/><TextBlock Text="48" Foreground="{DynamicResource TextHubPercentAchievementBrush}" FontSize="18" FontWeight="Bold"/></StackPanel></Border>
                                                    </UniformGrid>
                                                </Grid>
                                            </Border>

                                            <Grid Grid.Row="2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="30"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <StackPanel>
                                                    <TextBlock Text="Library News" Foreground="{DynamicResource TextHubBannerCardTitleBrush}" FontSize="21" FontWeight="Bold" Margin="8,0,0,8"/>
                                                    <Border Height="178" Background="{DynamicResource CardNewsBackgroundBrush}" BorderBrush="{DynamicResource CardNewsBorderBrush}" BorderThickness="1.2" CornerRadius="10" ClipToBounds="True">
                                                        <Grid>
                                                            <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="48"/></Grid.RowDefinitions>
                                                            <Grid>
                                                                <Border Background="{DynamicResource HubAchievementsBackgroundBrush}"/>
                                                                <Border Height="5" VerticalAlignment="Top" Background="{Binding PrimaryAccentBrush}"/>
                                                                <Border HorizontalAlignment="Left" VerticalAlignment="Top" Margin="12" Background="{Binding PrimaryAccentBrush}" CornerRadius="5" Padding="8,3">
                                                                    <TextBlock Text="NEWS" Foreground="{DynamicResource TextColorDarkBrush}" FontSize="8" FontWeight="Bold"/>
                                                                </Border>
                                                                <StackPanel Margin="16" VerticalAlignment="Bottom">
                                                                    <TextBlock Text="New content available" Foreground="{DynamicResource TextBrush}" FontSize="15" FontWeight="Bold"/>
                                                                    <TextBlock Text="A short secondary description appears here." Foreground="{DynamicResource TextAltBrush}" FontSize="9" Margin="0,4,0,0"/>
                                                                </StackPanel>
                                                            </Grid>
                                                            <Border Grid.Row="1" Background="{DynamicResource HubCardBottomBackgroundBrush}" BorderBrush="{DynamicResource HubCardBottomBorderBrush}" BorderThickness="0,1,0,0" Padding="12,8">
                                                                <TextBlock Text="Library update" Foreground="{DynamicResource TextHubBannerCardBottomBrush}" FontWeight="Bold"/>
                                                            </Border>
                                                        </Grid>
                                                    </Border>
                                                </StackPanel>

                                                <StackPanel Grid.Column="2">
                                                    <TextBlock Text="Latest News" Foreground="{DynamicResource TextHubBannerCardTitleBrush}" FontSize="21" FontWeight="Bold" Margin="8,0,0,8"/>
                                                    <Border Height="178" Background="{DynamicResource CardNewsBackgroundBrush}" BorderBrush="{DynamicResource CardNewsBorderBrush}" BorderThickness="1.2" CornerRadius="10" ClipToBounds="True">
                                                        <Grid>
                                                            <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="48"/></Grid.RowDefinitions>
                                                            <Grid>
                                                                <Border Background="{DynamicResource HubAchievementsBackgroundBrush}"/>
                                                                <Border Height="5" VerticalAlignment="Top" Background="{Binding SecondaryAccentBrush}"/>
                                                                <Border HorizontalAlignment="Left" VerticalAlignment="Top" Margin="12" Background="{Binding SecondaryAccentBrush}" CornerRadius="5" Padding="8,3">
                                                                    <TextBlock Text="UPDATE" Foreground="{DynamicResource TextColorDarkBrush}" FontSize="8" FontWeight="Bold"/>
                                                                </Border>
                                                                <StackPanel Margin="16" VerticalAlignment="Bottom">
                                                                    <TextBlock Text="Theme and community news" Foreground="{DynamicResource TextHighlightNewsViewBrush}" FontSize="15" FontWeight="Bold"/>
                                                                    <TextBlock Text="Highlighted and secondary text remain easy to compare." Foreground="{DynamicResource TextAltBrush}" FontSize="9" Margin="0,4,0,0"/>
                                                                </StackPanel>
                                                            </Grid>
                                                            <Border Grid.Row="1" Background="{DynamicResource HubCardBottomBackgroundBrush}" BorderBrush="{DynamicResource HubCardBottomBorderBrush}" BorderThickness="0,1,0,0" Padding="12,8">
                                                                <TextBlock Text="Latest article" Foreground="{DynamicResource TextHubBannerCardBottomBrush}" FontWeight="Bold"/>
                                                            </Border>
                                                        </Grid>
                                                    </Border>
                                                </StackPanel>
                                            </Grid>
                                        </Grid>

                                        <Grid Grid.Row="2">
                                            <Border Background="{DynamicResource BottomBar}"/>
                                            <Border Background="{DynamicResource GlassMainBrush}" Opacity="0.68"/>
                                            <Border Height="1.5" VerticalAlignment="Top" Background="{DynamicResource SeparatorBottomBarBrush}"/>
                                        </Grid>
                                    </Grid>
                                </Grid>
<Grid Visibility="{Binding IsChecked, ElementName=PreviewMenusTab, Converter={StaticResource BoolToVisibility}}" ClipToBounds="True">
                                    <Image Source="/AnikiPackCreator;component/Assets/ColorPackPreviewBackground.jpeg" Stretch="UniformToFill"/>
                                    <Border Background="{DynamicResource ShadeBackgroundDetails}"/>
                                    <Border Background="#A5000000"/>

                                    <Grid Height="58" VerticalAlignment="Top" Opacity="0.40">
                                        <Border Background="{DynamicResource TopBar}"/>
                                        <Border Background="{DynamicResource GlassMainBrush}" Opacity="0.65"/>
                                        <Border Height="1.5" VerticalAlignment="Bottom" Background="{DynamicResource SeparatorTopBarBrush}"/>
                                        <Grid Margin="18,0">
                                            <TextBlock Text="Player" Foreground="{DynamicResource TextBrush}" FontWeight="Bold" VerticalAlignment="Center"/>
                                            <TextBlock Text="Hub      Horizontal      Filter      Search      Tools      Extensions" Foreground="{DynamicResource TextAltBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            <TextBlock Text="12:39" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                        </Grid>
                                    </Grid>

                                    <Border Width="325" HorizontalAlignment="Left" Margin="22,12,0,12" Background="{DynamicResource BackgroundMenu}" BorderBrush="{DynamicResource MenuBorderBrush}" BorderThickness="1.3" CornerRadius="8" ClipToBounds="True">
                                        <Grid>
                                            <Grid.RowDefinitions><RowDefinition Height="74"/><RowDefinition Height="*"/><RowDefinition Height="44"/></Grid.RowDefinitions>

                                            <Grid Grid.Row="0" Margin="16,0">
                                                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                                <Ellipse Width="40" Height="40" Fill="{DynamicResource ButtonBackgroundNoFocus}" Stroke="{DynamicResource MenuBorderBrush}" StrokeThickness="1" VerticalAlignment="Center"/>
                                                <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                                                    <TextBlock Text="Player" Foreground="{DynamicResource TextBrush}" FontSize="21" FontWeight="Bold"/>
                                                </StackPanel>
                                            </Grid>

                                            <StackPanel Grid.Row="1" Margin="12,12">
                                                <Border Background="{DynamicResource ButtonBackgroundFocus}" BorderBrush="{DynamicResource FocusGameBorderBrush}" BorderThickness="1.8" CornerRadius="5" Padding="12,11">
                                                    <TextBlock Text="◎     Profile" Foreground="{DynamicResource TextBrush}" FontWeight="SemiBold"/>
                                                </Border>
                                                <TextBlock Text="◌     Notifications" Foreground="{DynamicResource TextBrush}" Margin="13,16,0,0"/>
                                                <TextBlock Text="◍     Friends" Foreground="{DynamicResource TextBrush}" Margin="13,16,0,0"/>
                                                <TextBlock Text="★     Achievements" Foreground="{DynamicResource TextBrush}" Margin="13,16,0,0"/>
                                                <TextBlock Text="☰     News" Foreground="{DynamicResource TextBrush}" Margin="13,16,0,0"/>
                                                <TextBlock Text="◧     Steam Store" Foreground="{DynamicResource TextBrush}" Margin="13,16,0,0"/>
                                                <TextBlock Text="▦     Extras" Foreground="{DynamicResource TextBrush}" Margin="13,16,0,0"/>
                                                <Border Height="1" Background="{DynamicResource SeparatorBrush}" Margin="8,16,8,0"/>
                                                <TextBlock Text="◒     Lock Screen" Foreground="{DynamicResource TextAltBrush}" Margin="13,14,0,0"/>
                                                <TextBlock Text="?     Help and Support" Foreground="{DynamicResource TextAltBrush}" Margin="13,14,0,0"/>
                                                <TextBlock Text="⚙     Settings" Foreground="{DynamicResource TextAltBrush}" Margin="13,14,0,0"/>
                                                <TextBlock Text="⏻     Power" Foreground="{DynamicResource TextAltBrush}" Margin="13,14,0,0"/>
                                            </StackPanel>

                                            <Grid Grid.Row="2">
                                                <Border BorderBrush="{DynamicResource SeparatorBrush}" BorderThickness="0,1,0,0"/>
                                                <Grid Margin="14,0">
                                                    <TextBlock Text="Menu" Foreground="{DynamicResource TextAltBrush}" VerticalAlignment="Center"/>
                                                    <TextBlock Text="B   Back" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                                </Grid>
                                            </Grid>
                                        </Grid>
                                    </Border>
                                </Grid>
<Grid Visibility="{Binding IsChecked, ElementName=PreviewDetailsTab, Converter={StaticResource BoolToVisibility}}" ClipToBounds="True">
                                    <Image Source="/AnikiPackCreator;component/Assets/ColorPackPreviewBackground.jpeg" Stretch="UniformToFill"/>
                                    <Border Background="{DynamicResource ShadeBackground}"/>
                                    <Border Background="{DynamicResource ShadeLeft}"/>
                                    <Border Background="{DynamicResource ShadeBottom}" Height="205" VerticalAlignment="Bottom"/>

                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="44"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
<Grid Grid.Row="0">
                                            <Border Background="{DynamicResource TopBar}"/>
                                            <Border Background="{DynamicResource GlassMainBrush}" Opacity="0.74"/>
                                            <Border Height="1.5" VerticalAlignment="Bottom" Background="{DynamicResource SeparatorTopBarBrush}"/>
                                            <TextBlock Text="STEAM" Foreground="{DynamicResource TextBrush}" FontSize="18" FontWeight="Bold" Margin="18,0,0,0" VerticalAlignment="Center"/>
                                        </Grid>

                                        <Grid Grid.Row="1">
                                            <StackPanel Margin="70,82,0,0" VerticalAlignment="Top" HorizontalAlignment="Left">
                                                <TextBlock Text="Yakuza 0" Foreground="{DynamicResource TextBrush}" FontSize="54" FontWeight="Bold"/>
                                                <Border Background="{DynamicResource TextHighlightBrush}" CornerRadius="4" Padding="12,4" HorizontalAlignment="Left" Margin="0,8,0,0">
                                                    <TextBlock Text="DIRECTOR'S CUT" Foreground="{DynamicResource TextColorDarkBrush}" FontSize="14" FontWeight="Bold"/>
                                                </Border>
                                            </StackPanel>
<Border Width="470" Height="212" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="24,0,0,28"
                                                    Background="{DynamicResource BackgroundMenu}" BorderBrush="{DynamicResource MenuBorderBrush}" BorderThickness="1.2" CornerRadius="12" Padding="12">
                                                <Grid>
                                                    <Grid.ColumnDefinitions><ColumnDefinition Width="98"/><ColumnDefinition Width="14"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
<Border Width="92" Height="132" CornerRadius="8" Background="{DynamicResource CardNewsBackgroundBrush}" BorderBrush="{DynamicResource CardNewsBorderBrush}" BorderThickness="1" VerticalAlignment="Top">
                                                        <Grid Margin="8">
                                                            <Border BorderBrush="{Binding SecondaryAccentBrush}" BorderThickness="1" CornerRadius="5" Opacity="0.75"/>
                                                            <Border Width="34" Height="5" Background="{Binding PrimaryAccentBrush}" CornerRadius="2.5" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,10"/>
                                                        </Grid>
                                                    </Border>

                                                    <Grid Grid.Column="2">
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="*"/>
                                                        </Grid.RowDefinitions>

                                                        <Border Background="{Binding SecondaryAccentBrush}" CornerRadius="4" Padding="6,2" HorizontalAlignment="Left">
                                                            <TextBlock Text="Ryu Ga Gotoku Studio · 2025" Foreground="{DynamicResource TextColorDarkBrush}" FontSize="9" FontWeight="SemiBold"/>
                                                        </Border>

                                                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,7,0,0">
                                                            <Border Background="{DynamicResource ButtonPlayColor}" BorderBrush="{DynamicResource FocusGameBorderBrush}" BorderThickness="1.2" CornerRadius="8" Padding="18,7" Margin="0,0,8,0">
                                                                <TextBlock Text="INSTALL" Foreground="{DynamicResource TextBrush}" FontSize="16" FontWeight="Bold"/>
                                                            </Border>
                                                            <Border Background="{DynamicResource ButtonDetailsColor}" BorderBrush="{DynamicResource MenuBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="14,7" Margin="0,0,8,0">
                                                                <TextBlock Text="DETAILS" Foreground="{DynamicResource TextBrush}" FontSize="12" FontWeight="Bold"/>
                                                            </Border>
                                                            <Border Width="34" Background="{DynamicResource ButtonBackgroundNoFocus}" CornerRadius="6" Margin="0,0,6,0"><TextBlock Text="i" Foreground="{DynamicResource TextBrush}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                                                            <Border Width="34" Background="{DynamicResource ButtonBackgroundNoFocus}" CornerRadius="6"><TextBlock Text="⚙" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                                                        </StackPanel>

                                                        <UniformGrid Grid.Row="2" Columns="3" Margin="0,10,0,0">
                                                            <Border Background="{DynamicResource HubAchievementsBackgroundBrush}" BorderBrush="{DynamicResource HubAchievementsBorderBrush}" BorderThickness="1" CornerRadius="7" Padding="8,6" Margin="0,0,7,0"><StackPanel><TextBlock Text="Time Played" Foreground="{DynamicResource TextAltBrush}" FontSize="8"/><TextBlock Text="10 minutes" Foreground="{DynamicResource TextBrush}" FontWeight="Bold"/></StackPanel></Border>
                                                            <Border Background="{DynamicResource HubAchievementsBackgroundBrush}" BorderBrush="{DynamicResource HubAchievementsBorderBrush}" BorderThickness="1" CornerRadius="7" Padding="8,6" Margin="0,0,7,0"><StackPanel><TextBlock Text="Last Played" Foreground="{DynamicResource TextAltBrush}" FontSize="8"/><TextBlock Text="27/04/2026" Foreground="{DynamicResource TextBrush}" FontWeight="Bold"/></StackPanel></Border>
                                                            <Border Background="{DynamicResource HubAchievementsBackgroundBrush}" BorderBrush="{DynamicResource HubAchievementsBorderBrush}" BorderThickness="1" CornerRadius="7" Padding="8,6"><StackPanel><TextBlock Text="Installed" Foreground="{DynamicResource TextAltBrush}" FontSize="8"/><TextBlock Text="86 GB" Foreground="{DynamicResource TextBrush}" FontWeight="Bold"/></StackPanel></Border>
                                                        </UniformGrid>
<Border Grid.Row="3" Background="{DynamicResource HubAchievementsBackgroundBrush}" BorderBrush="{DynamicResource HubAchievementsBorderBrush}" BorderThickness="1" CornerRadius="7" Padding="9,8" Margin="0,9,0,0">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                                                <TextBlock Text="Achievements" Foreground="{DynamicResource TextBrush}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                                                <Grid Grid.Column="1" Margin="12,0" Height="10" VerticalAlignment="Center">
                                                                    <Border Background="{DynamicResource ProgressBackgroundBrush}" CornerRadius="5"/>
                                                                    <Border Width="92" HorizontalAlignment="Left" Background="{DynamicResource ProgressBrush}" CornerRadius="5"/>
                                                                </Grid>
                                                                <TextBlock Grid.Column="2" Text="42%" Foreground="{Binding PrimaryAccentBrush}" FontWeight="Bold" VerticalAlignment="Center"/>
                                                            </Grid>
                                                        </Border>

                                                        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,9,0,0">
                                                            <Border Background="{DynamicResource HubButtonBackgroundNoFocusBrush}" CornerRadius="7" Padding="9,4" Margin="0,0,7,0"><TextBlock Text="Action / Adventure" Foreground="{DynamicResource TextHubBannerCardBottomBrush}"/></Border>
                                                            <Border Background="{DynamicResource HubButtonBackgroundNoFocusBrush}" CornerRadius="7" Padding="9,4"><TextBlock Text="Single Player" Foreground="{DynamicResource TextHubBannerCardBottomBrush}"/></Border>
                                                        </StackPanel>
                                                    </Grid>
                                                </Grid>
                                            </Border>
<Border Width="296" Height="174" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,34,32"
                                                    Background="{DynamicResource CardNewsBackgroundBrush}" BorderBrush="{DynamicResource CardNewsBorderBrush}" BorderThickness="1.2" CornerRadius="12" Padding="10">
                                                <Grid>
                                                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                                                    <TextBlock Text="TRAILER" Foreground="{DynamicResource TextBrush}" FontSize="11" FontWeight="Bold"/>
                                                    <Border Grid.Row="1" Margin="0,8,0,0" Background="#FF252B31" BorderBrush="{DynamicResource FocusGameBorderBrush}" BorderThickness="1.5" CornerRadius="6">
                                                        <Grid Margin="14">
                                                            <Border BorderBrush="{Binding SecondaryAccentBrush}" BorderThickness="1" CornerRadius="5" Opacity="0.72"/>
                                                            <Border Height="5" VerticalAlignment="Top" Background="{Binding PrimaryAccentBrush}" CornerRadius="2.5"/>
                                                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                                                <TextBlock Text="MEDIA PREVIEW" Foreground="{DynamicResource TextHighlightBrush}" FontSize="19" FontWeight="Bold" HorizontalAlignment="Center"/>
                                                                <TextBlock Text="Card / border / accent" Foreground="{DynamicResource TextAltBrush}" FontSize="9" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                                            </StackPanel>
                                                        </Grid>
                                                    </Border>
                                                </Grid>
                                            </Border>
                                        </Grid>
                                    </Grid>
                                </Grid>
                            </Grid>
                        </Viewbox>
                    </Border>

                    <Border Grid.Row="4" Background="#FF111B22" BorderBrush="#FF2E3B45" BorderThickness="1" CornerRadius="6" Padding="12">
                        <TextBlock Text="{DynamicResource ColorTemplateSafetyNote}" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="4" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel>
                        <TextBlock Text="{DynamicResource ColorPackSummary}" Style="{StaticResource SectionTitle}"/>

                        <TextBlock Text="{DynamicResource ColorGeneratedTitle}" FontWeight="SemiBold" Margin="0,4,0,5"/>
                        <TextBlock Text="{Binding GeneratedColorCountText}" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" FontSize="11"/>

                        <TextBlock Text="{DynamicResource ColorExportTitle}" FontWeight="SemiBold" Margin="0,18,0,5"/>
                        <TextBlock Text="{DynamicResource ColorExportDescription}" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" FontSize="11"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Margin="0,18,0,0">
                        <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" MaxHeight="52" Margin="0,0,0,10"/>
                        <Button Content="{DynamicResource ExportColorPackZip}"
                                Click="ExportZipClick"
                                Background="#FF7A5826"
                                BorderBrush="{StaticResource AccentBrush}"
                                Padding="14,9"/>
                        <Button Content="{DynamicResource ShareCommunityPack}"
                                Click="ShareCommunityPackClick"
                                Style="{StaticResource SecondaryButton}"
                                Padding="14,9"
                                Margin="0,8,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
